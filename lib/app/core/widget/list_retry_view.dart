import '/app/core/exporter.dart';

class ListRetryView extends StatelessWidget {
  const ListRetryView({
    super.key,
    required void Function() onRetry,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
